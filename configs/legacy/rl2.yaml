training:
  episodes: 60
  episode_orders: 10000
  lr: 0.001
  gamma: 0.99
  batch_size: 256
  target_update_steps: 2000
  train_start_size: 3000
  train_every_steps: 4
  updates_per_episode: 500
  ckpt_every: 10

epsilon:
  start: 1.0
  end: 0.05
  decay_steps: 200000

network:
  input_dim: 8
  hidden_dim: 64

buffer:
  capacity: 200000

reward:
  reward_mode: urgent_protection   # +10 urgent on-time | -5 urgent late | +1 normal on-time | 0 normal late

evaluation:
  every_episodes: 10
  seeds: 3

scenario_mix:
  enabled: true
  scenarios:
    - name: s211
      prob: 0.6
      workers: [2, 1, 1]
      dec_cap: 4500
    - name: s111
      prob: 0.3
      workers: [1, 1, 1]
      dec_cap: 3000
    - name: s221
      prob: 0.1
      workers: [2, 2, 1]
      dec_cap: 6000
